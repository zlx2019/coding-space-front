
/**
 * 在JavaSript中,可以通过 typeof 操作符来获取一个变量的类型
 * 如: var typoe = typeof 123
 */


var age = 18

// 获取age的类型
var type =  typeof age

// 判断age的类型是否为number
var isNumber = type === 'number'
console.log(isNumber);